* Add CSRF to events forms

* Form fields require correction

* Create Location and Category CRUD

* If event's size reached the max, cannot join, notice message

* If user deletes self, event set null and possibility to take the ownership from other users

* Build in front end

* Comments, Reviews

* Setup emails

* Make description for classes, functions etc.

* Deploy

* Show in Portfolio